/* -------------------------------------------
 * Colors
 ------------------------------------------ */

for color in $sr-colors
  .{'%s' % (color[0])} // s('%s', $varb)
    c(color[1])
  .b{'%s' % (color[0])} // s('%s', $varb)
    bc(color[1])

